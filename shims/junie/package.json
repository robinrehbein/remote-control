{
  "name": "@pocketagent/shim-junie",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PocketAgent adapter shim wrapping the Junie CLI (one-shot process per prompt) behind the unified HTTP protocol",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p . --noEmit",
    "smoke": "tsx smoke/smoke.ts"
  },
  "dependencies": {
    "@pocketagent/protocol": "file:../../packages/protocol",
    "@types/node": "^22.15.0",
    "fastify": "^5.2.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22"
  }
}
