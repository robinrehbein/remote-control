{
  "name": "pocketagent",
  "version": "0.2.0",
  "private": true,
  "description": "PocketAgent v2 (pi-only): Android-Fernbedienung für den pi-Coding-Agenten — Orchestrator, Runner-Image und Link-Agent",
  "type": "module",
  "workspaces": [
    "packages/*",
    "server",
    "link"
  ],
  "scripts": {
    "typecheck": "tsc -p packages/protocol && tsc -p server && tsc -p link && tsc -p runner",
    "test": "npm run test -w packages/protocol --if-present && npm run test -w link --if-present",
    "smoke:server": "npm run smoke -w server --if-present",
    "smoke:runner": "cd runner && npm run smoke",
    "smoke:link": "npm run smoke -w link --if-present",
    "smoke:real": "cd runner && npm run smoke:real"
  },
  "engines": {
    "node": ">=22"
  }
}
