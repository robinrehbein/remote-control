{
  "$schema": "https://pocketagent.dev/schemas/adapter.json",
  "id": "claude",
  "name": "Claude Code",
  "description": "Claude Code via Agent SDK; Pro/Max subscription through setup-token or Anthropic API key.",
  "capabilities": { "approvals": true, "resume": true, "streaming": true, "autoPush": true, "reasoning": true, "modelSwitch": true },
  "credentials": {
    "claude_oauth": ["CLAUDE_CODE_OAUTH_TOKEN"],
    "anthropic": ["ANTHROPIC_API_KEY"]
  },
  "providerEnv": {},
  "providers": [
    {
      "id": "claude_oauth",
      "name": "Claude Abo (Setup-Token)",
      "hint": "Auf dem Laptop `claude setup-token` ausführen und den Token einfügen (Pro/Max, ~1 Jahr gültig) — oder direkt `pocketagent-secret claude`"
    },
    {
      "id": "anthropic",
      "name": "Anthropic",
      "keyUrl": "https://console.anthropic.com/settings/keys",
      "hint": "API-Key von console.anthropic.com (sk-ant-…), Abrechnung pro Token"
    }
  ],
  "defaults": { "provider": "anthropic", "model": "" }
}
